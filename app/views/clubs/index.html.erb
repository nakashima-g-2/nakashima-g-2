<h1>部活動一覧</h1>

<table class="table table-striped table-bordrerd">
  <tr>
    <td>部活動名</td>
    <td>操作</td>
  </tr>
  <% @clubs.each do|club| %>
    <tr>
      <td><%= club.name %></td>
    </tr>
  <% end %>
</table>
<br>

  <%= paginate @clubs %>

<p style="text-align:center"><%= link_to '新規登録', new_club_path, class: "btn btn-primary" %></p>
<p style="text-align:center"><%= link_to 'トップに戻る', root_url, class: "btn btn-primary" %></p>
